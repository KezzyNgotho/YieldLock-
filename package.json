{
  "name": "yieldlock-plus",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "deploy:base": "hardhat run scripts/deploy.js --network base",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "node": "hardhat node",
    "frontend:install": "cd frontend/yieldlock-ui && npm install",
    "frontend:start": "cd frontend/yieldlock-ui && npm start",
    "frontend:build": "cd frontend/yieldlock-ui && npm run build"
  },
  "keywords": [
    "defi",
    "chainlink",
    "savings",
    "ai",
    "yield"
  ],
  "author": "YieldLock+ Team",
  "license": "MIT",
  "description": "Smart goal-based DeFi savings platform powered by AI and Chainlink",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.0.0",
    "dotenv": "^17.0.0",
    "ethers": "^6.14.4",
    "hardhat": "^2.25.0"
  },
  "dependencies": {
    "@chainlink/contracts": "^1.4.0",
    "@openzeppelin/contracts": "^5.3.0"
  }
}
